//! `ts-subtitles` — крейт-плейсхолдер для субтитров Timeline Studio.
//!
//! На момент выноса (#99) subtitle-**логика** жила в Tauri-commands (остались в
//! монолите), а subtitle-**типы** — в `ts-schema` (`schema::subtitles`). Поэтому
//! модуль пока пуст и без тестов. Когда появится платформенно-независимая
//! subtitle-логика (парсинг SRT/VTT, сдвиг таймингов, рендер) — она ляжет сюда + юнит-тесты.
pub mod subtitles;
