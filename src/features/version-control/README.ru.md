# Version Control

[English](./README.md) | **Русский**

## Обзор
Модуль контроля версий для снапшотов проекта, управления ветками, восстановления версий и отслеживания изменений с автосохранением.

## Статус
- ✅ **Готовность**: 100% - Полностью реализовано и готово к продакшену
- ✅ **Компоненты**: 2 UI компонента для управления версиями (полностью протестированы)
- ✅ **Хуки**: 1 основной хук для операций контроля версий (32 теста)
- ✅ **Сервисы**: Интеграция через унифицированный backend-sync сервис
- ✅ **Тесты**: 76 тестов (44 компонентных + 32 хука)
- ✅ **i18n**: Полная локализация (15 языков)

## Структура
```
version-control/
├── components/
│   ├── version-control-manager.tsx
│   └── version-history-panel.tsx
├── hooks/
│   └── use-version-control.ts
├── types.ts
└── __tests__/
    └── integration.test.ts
```

## Функции
### ✅ Реализовано
- [x] Создание снапшотов с опциональными сообщениями
- [x] Восстановление любой сохраненной версии
- [x] Просмотр истории версий с метаданными
- [x] Сравнение различий между версиями
- [x] Создание веток от текущей или указанной версии
- [x] Переключение между ветками
- [x] Отслеживание несохраненных изменений
- [x] Автоматические снапшоты с настраиваемым интервалом
- [x] Включение/отключение автосохранения
- [x] UI интеграция в модальном окне User Settings

### ❌ Не реализовано
- [ ] Слияние веток (запланировано для фазы 2)
- [ ] Экспорт/импорт истории версий (запланировано для фазы 2)

## Использование
```typescript
import { useVersionControl } from '@timeline-studio/ui/features/version-control'

function ProjectHeader() {
  const {
    currentVersionId,
    branchName,
    hasUncommittedChanges,
    createSnapshot,
    restoreVersion,
    switchBranch
  } = useVersionControl()

  const handleSave = async () => {
    await createSnapshot("Изменения макета")
  }

  return (
    <div>
      <Badge>{branchName}</Badge>
      {hasUncommittedChanges && <Badge variant="destructive">Несохраненные</Badge>}
      <Button onClick={handleSave}>Сохранить версию</Button>
    </div>
  )
}
```

## Интеграция
- **Зависит от**: @/domains/project-management (backend sync), Унифицированная система команд
- **Используется в**: Модальное окно User Settings → вкладка Version Control

## Тестирование
- **Всего тестов**: 76 тестов
- **Тесты компонентов**: 44 теста
- **Тесты хуков**: 32 теста

```bash
bun run test src/features/version-control/__tests__/
```

## TODO / Roadmap
- [ ] Функциональность слияния веток (требует поддержку backend)
- [ ] Экспорт/импорт истории версий в файлы
- [ ] Поддержка мульти-GPU рендеринга
- [ ] Облачное хранилище версий для синхронизации
- [ ] UI визуализации различий версий
- [ ] Умные триггеры автосохранения (при крупных изменениях)
- [ ] E2E тесты (запланированы в `e2e/tauri/features/version-control/`)
