# Motion Graphics

[English](./README.md) | **Русский**

## Обзор

Профессиональная система анимации на основе ключевых кадров для Timeline Studio, позволяющая создавать сложные анимации свойств объектов во времени, подобно After Effects или другим профессиональным инструментам моушн-дизайна.

## Статус

- ✅ **Компоненты**: Motion Graphics Panel, Curve Editor
- ✅ **Сервисы**: Управление keyframe, алгоритмы интерполяции, движок выражений
- ✅ **Интеграция**: Интеграция с Timeline клипами с анимационными данными
- ⚠️ **Тесты**: 0 тестовых файлов (только ручное и интеграционное тестирование)

## Структура

```
motion-graphics/
├── components/
│   ├── motion-graphics-panel.tsx  # Главная панель управления
│   └── curve-editor.tsx          # Визуальный редактор кривых
├── services/
│   ├── keyframe-manager.ts       # CRUD операции с keyframe
│   ├── interpolation.ts          # Алгоритмы интерполяции
│   ├── expression-engine.ts      # Вычислитель JavaScript выражений
│   ├── animation-layers.ts       # Система управления слоями
│   ├── preset-manager.ts         # Загрузка и управление пресетами
│   └── timeline-integration.ts   # Интеграция с Timeline клипами
├── types/
│   └── keyframe.ts              # TypeScript определения
├── data/
│   └── motion-presets.json      # Встроенные пресеты анимации
└── hooks/
    └── use-motion-graphics.ts   # React хук для компонентов
```

## Возможности

### ✅ Реализовано

- [x] Система анимации по ключевым кадрам (создание, редактирование, удаление)
- [x] Множество типов интерполяции (Linear, Bezier, Ease, Bounce, Elastic, Back, Expo, Hold)
- [x] JavaScript движок выражений для процедурной анимации
- [x] Встроенные функции выражений (математика, сглаживание, шум, волны, векторы, цвета)
- [x] Система слоёв анимации с режимами смешивания
- [x] Визуальный редактор кривых для точного контроля
- [x] Пресеты анимации (текст, переходы, трансформации, эффекты, поведения)
- [x] Интеграция с Timeline (применение анимаций к клипам)
- [x] Предпросмотр и воспроизведение в реальном времени
- [x] Копирование/вставка анимаций между клипами
- [x] Импорт/экспорт анимационных данных

### ❌ Не реализовано

- [ ] Симуляция размытия движения
- [ ] UI расширенного редактора кривых сглаживания
- [ ] Автодополнение выражений
- [ ] Маркетплейс шаблонов анимации
- [ ] GPU ускорение для сложных анимаций
- [ ] Импорт данных motion capture

## Использование

### Базовая анимация

```typescript
import { createKeyframe, addKeyframeToProperty } from '@/features/motion-graphics/services/keyframe-manager'

// Создание анимации прозрачности
const property: AnimatedProperty = {
  id: 'opacity',
  name: 'Прозрачность',
  path: 'opacity',
  type: 'number',
  keyframes: [],
  enabled: true
}

// Добавление ключевых кадров
property = addKeyframeToProperty(property, createKeyframe(0, 0, 'ease-out'))
property = addKeyframeToProperty(property, createKeyframe(1, 1, 'linear'))
```

### Движок выражений

```typescript
import { ExpressionEvaluator } from '@/features/motion-graphics/services/expression-engine'

const evaluator = new ExpressionEvaluator()
const result = evaluator.evaluate(
  'value + sin(time * 2 * Math.PI) * 50',
  { time: 1.5, frame: 45, fps: 30, value: 100 }
)
```

### Интеграция с Timeline

```typescript
import { applyMotionToClip } from '@/features/motion-graphics/services/timeline-integration'

// Добавление анимации к клипу
const animatedClip = applyMotionToClip(clip, animationTrack)

// Вычисление анимации в конкретное время
const values = evaluateClipMotionAtTime(animatedClip, 1.5)
// Возвращает: { opacity: 0.75, position: [100, 200], ... }
```

## Типы интерполяции

- **Linear** - Постоянная скорость между ключевыми кадрами
- **Bezier** - Плавные кривые с настраиваемыми маркерами
- **Hold** - Без интерполяции (мгновенное изменение)
- **Ease** - Плавное ускорение/замедление
- **Ease In** - Медленное начало, быстрый конец
- **Ease Out** - Быстрое начало, медленный конец
- **Ease In Out** - Медленно с обоих концов
- **Bounce** - Физика прыгающего мяча
- **Elastic** - Пружинистое превышение
- **Back** - Предвосхищение и превышение
- **Expo** - Экспоненциальное ускорение

## Интеграция

- **Зависит от**: React 19+, Canvas API (редактор кривых)
- **Используется в**: Timeline клипы, требующие анимации
- **Только frontend**: Без Tauri backend команд

## Тестирование

- **Всего тестов**: 0 тестовых файлов
- **Подход к тестированию**: Ручное тестирование в UI Timeline Studio, интеграционное тестирование с модулем Timeline, визуальное тестирование анимаций

Будущее покрытие тестами должно включать:
- Алгоритмы интерполяции keyframe
- Вычисление движка выражений
- Применение и настройка пресетов
- Интеграция с Timeline

Запуск тестов:
```bash
# В настоящее время нет специализированных тестов
bun run test src/features/motion-graphics
```

## Производительность

- Выражения компилируются и кэшируются
- Оптимизированные алгоритмы интерполяции
- Эффективный композитинг слоёв
- Вычисляются только видимые свойства
- Умное кэширование вычисленных значений

## TODO / Roadmap

- [ ] Добавить комплексные юнит тесты для алгоритмов интерполяции
- [ ] Реализовать симуляцию размытия движения
- [ ] Создать UI расширенного редактора кривых сглаживания
- [ ] Добавить автодополнение и подсветку синтаксиса выражений
- [ ] Построить маркетплейс шаблонов анимации
- [ ] Реализовать GPU ускорение для сложных анимаций
- [ ] Добавить поддержку импорта данных motion capture
- [ ] Создать визуальные регрессионные тесты для анимаций
